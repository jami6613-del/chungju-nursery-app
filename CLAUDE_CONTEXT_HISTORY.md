# Claude Code 인계용 히스토리 (Season Orders / Daily Todos / Dashboard / Planning)

이 문서는 **Claude Code**(또는 다른 코딩 에이전트)가 현재 프로젝트(`my first vibe`)를 이어서 개발/수정할 수 있도록, 기존 대화/요구사항/구현/SQL/주의사항을 한 번에 정리한 인계서입니다.

## 1) 프로젝트/환경 요약

- **OS/쉘**: Windows 10, PowerShell
- **워크스페이스**: `c:\Users\PC\Desktop\my first vibe`
- **Git**: 현재 폴더는 git repo 아님(사용자 환경 정보 기준)
- **주요 스택**: React + TypeScript + Tailwind, Supabase
- **핵심 파일**: `app/src/App.tsx`에 페이지/라우팅/대부분의 UI 로직이 집중되어 있음

## 2) 큰 작업 흐름 (무엇을 했는가)

1. 메인메뉴 전광판(오늘/내일 할 일) 기능 확장  
2. 신규 메뉴 **“시즌 작물 주문현황”** 추가(5개의 가로 스크롤 화이트보드)  
3. 시즌 작물 주문현황의 UX 개선(정렬/검색/단위/소수점/비고 뱃지/판매완료/폰번호 정규화 등)  
4. 시즌 주문 데이터의 **localStorage → Supabase 동기화** 마이그레이션  
5. **재고관리(Inventory)** 기능 추가(Supabase) 및 “관리자만 버튼 노출” 제한  
6. 대시보드(파종 및 출하현황) 및 주문/파종계획 일부 UX 디테일 개선(스트라이프 표시, 야외경화 일차 수정, 페이지 이동 스크롤, 페이지 빨간점 등)

## 3) 요구사항/동작 상세 (구현된 내용)

### A. 메인메뉴 “오늘의 할 일 / 내일의 할 일”

- **자정(00:00) 자동 롤오버**
  - 오늘 할 일은 사라짐
  - 내일 할 일은 오늘 할 일로 이동
  - 단, **완료(삭선)된 항목은 롤오버 시 제외**(즉, 다음날로 넘어가지 않음)
- **터치 시 “완료” 버튼 → 삭선 처리**
  - 삭제가 아니라 완료 표시(삭선)
  - 오늘/내일 전광판 모두 동일 기능 제공(미리 완료할 가능성 고려)
- **편집 모달에서 completed 상태 유지**

관련 API 타입 변경:
- `app/src/lib/dailyTodosApi.ts`
  - `DailyTodoItem = { text: string; completed: boolean }` 형태로 저장/로드

### B. 신규 메뉴 “시즌 작물 주문현황”(Season Orders)

- **메인메뉴 버튼 추가**
  - “육묘확인서 발급” 아래에 **“시즌 작물 주문현황”** 버튼 생성
  - 라우트: `/season-orders` (App.tsx 라우팅에 연결)

- **화면 구성**
  - “주문 및 파종계획”의 화이트보드 UX를 참고하되, **가로 스크롤 가능한 5개 보드**
  - 각 보드 상단 헤더(기존 날짜 영역)는 **터치 → 작물명 입력 팝업**
  - 보드 우측에 `+` 버튼 → 주문 항목 추가
  - 항목 탭 → 수정/삭제 가능
  - 항목이 많으면 **보드 안에서 세로 스크롤**
  - 하단은 “트레이 종류별 합계” 삭제하고, 대신 **“품종별 수량 합계”**를 표시

- **입력 필드(초기 요구)**
  - 주문자, 품종, 수량, 연락처, 비고(요구 변동 있음: 아래 “비고” 참고)
  - 전부 타이핑 입력

#### B-1. 표시/편집 UX 디테일

- **비고(note) 처리**
  - 리스트 한 줄 유지 목적: “비고”를 **추가 폼/리스트에서 제거**
  - 이후 요구 변경: **편집 모달에서만 비고 입력을 허용**하고,
    - 리스트에는 “비고” 뱃지로 존재 여부만 표시(작게)
    - 뱃지 텍스트는 “비고 확인” → **“비고”**
    - 뱃지 색상은 눈에 띄게(예: fuchsia 계열)

- **폰트 크기**
  - 시즌 주문 리스트의 폰트 크기를 **20% 증가**

- **연락처 표시 정규화**
  - 숫자만 연속 입력, 하이픈 포함 입력 모두 최종 표시는 **`000-0000-0000`**
  - 부분 입력도 `010-XXXX-XXXX` 형태로 보정 요구(예: `12345678` → `010-1234-5678`)
  - 구현은 `normalizeKoreanMobile` 계열 유틸로 처리(아래 API 파일 참고)

- **정렬/검색**
  - 기본: 품종별 자동 정렬
  - 토글 추가: **“주문자별 정렬” / “품종별 정렬”**
  - **주문자 검색** 필터 추가
    - 필터가 켜졌을 때 상단에 **“전체주문 보기”**(라임 그린 등) 버튼 표시 → 필터 해제

- **수량 단위(판/포기)**
  - 추가/수정 모달의 수량 입력 옆에 **단위 선택 버튼(“판”/“포기”)**
  - 리스트 표시:
    - “포기”는 `50 포기`처럼 단위를 붙여 표시
    - “판”은 숫자만 표시(기존 UX 유지)

- **판매완료(sold)**
  - 편집 모달에 **판매완료 토글 버튼**
  - sold=true면 리스트에서 row를 **dim 처리**
  - sold 항목은 **품종별 합계 계산에서 제외**

#### B-2. 레이아웃(모바일에서 합계가 안 보이던 문제 해결 이력)

문제:
- iPhone/모바일에서 보드 리스트가 화면을 과도하게 차지하여 **하단 “품종별 수량 합계”가 화면 밖으로 밀려 보이지 않음**
- 요구: 하단 합계 영역은 **항상 보이고**, 리스트만 스크롤되어야 함

해결 과정 요약(여러 번의 시행착오 후 최종 형태):
- flex/absolute/grid 등 여러 접근을 시도했지만 iOS에서 높이 계산이 흔들림
- 최종적으로는 화면을 고정 높이(`100dvh` 등)로 잡고, **화이트보드(리스트) 영역과 합계 영역을 “퍼센트 높이로 분할”**하는 방식으로 안정화
  - 사용자 요구에 맞춰 비율 조정:
    - 리스트(화이트보드) 영역: **78%**
    - 품종합계창: **23%**
  - 합계창은 in-flow로 하단에 배치(리스트만 `overflow-y-auto`)

### C. 데이터 저장/동기화 (localStorage → Supabase 마이그레이션)

초기 상태:
- 시즌 주문현황 데이터가 `localStorage`에 저장되어 **다른 기기/계정과 동기화가 안 됨**

요구:
- 데이터를 Supabase로 옮기고 이후 모든 사용자가 동일 데이터 조회/편집
- 기존 로컬 데이터는 유실되지 않게 **마이그레이션** 필요
- 마이그레이션은 **수동 트리거/버튼** 및 에러 메시지 명확히
- 마이그레이션 완료 후 “로컬데이터 업로드” 버튼 제거

이슈/교훈:
- 마이그레이션 실패 원인 중 하나가 **RLS 정책에서 INSERT 권한 누락**이었음(특히 boards upsert 시)
- 실패 메시지를 “이관 중 오류”로 뭉개지 말고, Supabase 에러 메시지를 그대로 표시하도록 개선
- MIGRATED_FLAG 같은 플래그가 이미 찍혀있어도, 서버가 비어있다면 재이관이 가능하도록 방어 로직 필요(사용자가 “데이터가 사라졌다”고 느끼는 케이스 대응)

### D. 재고관리(Inventory)

요구:
- “로컬데이터 업로드” 버튼 제거 후 그 자리에 **“재고관리”** 버튼 배치
- 클릭 시 모달/창 오픈
- **현재 보고 있는 화이트보드(visible board)**에 등장하는 품종들을 기준으로 재고를 관리
  - 다른 보드를 보고 눌렀으면 그 보드 품종 기준으로 재고관리
- 모달 내 표시 컬럼:
  - 재고(판), 삭감(판), 잔여(판)
  - 재고(포기), 삭감(포기), 잔여(포기)
- 초기에는 UI가 미흡하다고 사용자 피드백 → **일단 관리자만 버튼 보이게 제한**
  - 조건: `user.role_level === 0` 일 때만 “재고관리” 버튼 렌더

### E. 대시보드(파종 및 출하현황) / 계획 화면 디테일

- **대시보드 줄무늬(경고) 표시**
  - 주문이 “출하완료”인데 `파종수량` != `출하수량`이면, 해당 row 전체에 대각 스트라이프 오버레이
  - 투명도는 텍스트가 읽히게 조절(사용자 요구로 75%까지 상향)
  - 수량이 나중에 일치하면 스트라이프 제거

- **야외경화 일차 수정**
  - 야외경화 상태 표시의 주황색 원(“N일째”)을 **2초 롱프레스**하면 팝업
  - 팝업:
    - 제목: “야외경화 일차 수정”
    - 문구: “야외 경화 일차를 몇 일차로 변경할까요?”
    - 숫자만 입력 가능, 확인/취소 버튼
  - 변경 시 `outdoor_hardening_at`가 수정되어 N일째 표기 반영

- **주문 및 파종계획 페이지네이션 UX**
  - 페이지 숫자를 눌러 이동하면, 해당 페이지의 **상단으로 스크롤**되도록 처리(이전 페이지 스크롤 위치가 유지되어 하단이 보이던 문제 개선)
  - “아직 파종계획에 반영되지 않은 주문”이 위치한 페이지 숫자에 **빨간점** 표시(우상단 등 작은 점)

## 4) 실제로 수정/추가된 주요 파일 목록(찾기용)

### 프론트엔드(React)

- `app/src/App.tsx`
  - 메인 메뉴 버튼/라우팅 추가(`/season-orders`)
  - `SeasonOrdersPage` 컴포넌트(5개 보드, 추가/수정/삭제 모달, 정렬/검색, 비고 뱃지, sold 처리)
  - 레이아웃(리스트 78% + 합계 23% 고정 분할)
  - 재고관리 버튼(관리자만 노출) + 재고관리 모달
  - PlanningPage: 야외경화 롱프레스 수정 팝업, 페이지 이동 시 스크롤 상단 처리, 빨간점 표시
  - DashboardPage: 출하완료+수량불일치 row 스트라이프 표시

- `app/src/lib/dailyTodosApi.ts`
  - `DailyTodoItem` 타입 확장 및 저장/로드 형태 변경

- `app/src/lib/seasonOrdersApi.ts`
  - 시즌 주문 데이터 모델/CRUD
  - `normalizeKoreanMobile`(연락처 표시 보정)
  - localStorage 스냅샷 추출 + Supabase 마이그레이션 함수
  - 재고 API(`fetchSeasonInventory`, `upsertSeasonInventory`)

- `app/src/components/ui.tsx`
  - `TextField`에 `inputMode`, `pattern` 전달 가능하도록 확장(소수점 입력 UX 보완)

### Supabase SQL(실행/적용 필요했던 스크립트들)

- `app/supabase-season-orders.sql`
  - `season_orders_boards`, `season_orders_items` 테이블 생성
  - RLS 정책(읽기/쓰기/업데이트/삭제)
  - **boards에 INSERT 정책이 없으면 마이그레이션/upsert가 실패할 수 있음**

- `app/supabase-season-inventory.sql`
  - `season_inventory` 테이블 생성(보드+품종 PK)
  - RLS 정책(읽기/insert/update)

- `app/supabase-orders-allow-decimal.sql`
  - `orders.quantity_base`, `orders.quantity_extra`, `orders.shipping_quantity` 등을 `numeric`으로 변경(소수점 저장)

## 5) 운영/디버깅 메모(중요)

- **PowerShell 명령 체인**
  - `npm run build && ...` 형태는 PowerShell에서 실패할 수 있어 `;` 사용이 필요했던 이력이 있음

- **마이그레이션 실패 시 1순위 점검**
  - Supabase RLS 정책: `season_orders_boards`에 **INSERT 정책**이 있는지(업서트가 insert를 필요로 함)
  - Supabase 에러 메시지를 UI에서 그대로 확인하도록 되어있는지(“이관 중 오류” 뭉개기 금지)
  - “데이터가 사라졌다” 케이스: 실제로는 **localStorage에 남아있는데 Supabase가 비어있는** 상황일 수 있음(재이관 로직/플래그 처리 주의)

- **iOS 높이/스크롤**
  - iOS Safari는 flex/grid의 `100vh`/`1fr` 조합이 불안정할 수 있어,
  - “리스트 영역 + 합계 영역”을 **명시적인 퍼센트 높이로 나눠** 안정화한 이력이 있음(78%/23%)

## 6) 참고(대화 로그)

- 원본 대화 JSONL 경로가 사용자 환경에 존재함(에이전트 인계 목적).  
  이 문서가 충분하다면 굳이 JSONL을 직접 파싱하지 않아도 됨.

